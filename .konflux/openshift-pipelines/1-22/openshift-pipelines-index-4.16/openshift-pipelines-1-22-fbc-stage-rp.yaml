# Generated for Konflux Application openshift-pipelines-index-4.16 by openshift-pipelines/hack. DO NOT EDIT
apiVersion: appstudio.redhat.com/v1alpha1
kind: ReleasePlan
metadata:
  labels:
    release.appstudio.openshift.io/auto-release: "false"
    release.appstudio.openshift.io/standing-attribution: "true"
    release.appstudio.openshift.io/releasePlanAdmission: openshift-pipelines-1-22-fbc-stage
  name: openshift-pipelines-1-22-fbc-stage-rp
spec:
  application: openshift-pipelines-index-4-16-1-22
  target: rhtap-releng-tenant
  data:
    mapping:
      defaults:
        tags:
          - v1.22.2
          - v1.22.2-{{ timestamp }}
    releaseNotes:
      references:
        - "https://docs.redhat.com/en/documentation/red_hat_openshift_pipelines"
      type: "RHEA"
      solution: |
        Red Hat OpenShift Pipelines is a cloud-native, continuous integration and
        continuous delivery (CI/CD) solution based on Kubernetes resources.
        It uses Tekton building blocks to automate deployments across multiple
        platforms by abstracting away the underlying implementation details.
        Tekton introduces a number of standard custom resource definitions (CRDs)
        for defining CI/CD pipelines that are portable across Kubernetes distributions.
      description: "The 1.22.2 release of Red Hat OpenShift Pipelines Operator."
      topic: |
        The 1.22.2 GA release of Red Hat OpenShift Pipelines Operator..
        For more details see [product documentation](https://docs.redhat.com/en/documentation/red_hat_openshift_pipelines).
      synopsis: "Red Hat OpenShift Pipelines Release 1.22.2"
  collectors:
    serviceAccountName: tekton-ecosystem-collectors-sa
    secrets:
      - jira-collectors-secret
    items:
      - name: jira-collector
        params:
          - name: url
            value: https://issues.redhat.com
          - name: query
            value: 'project = SRVKP AND fixVersion = "Pipelines 1.22.2" AND component NOT IN (QA, Performance, p12n, Operator, "Tekton CLI", "OpenShift OPC") AND (status = "Release Pending" OR (status = Closed AND resolution = Done)) AND (labels is EMPTY OR labels != "release-testing-bug") AND (issuetype = Epic OR ((issuetype IN (Story, Bug)) AND "Epic Link" is EMPTY))'
          - name: secretName
            value: jira-collectors-secret
        timeout: 120
        type: jira